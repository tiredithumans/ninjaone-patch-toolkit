# NinjaOne API surface used by this toolkit

Generated by `scripts/ninjaone-spec-digest.py` from NinjaOne's published OpenAPI spec. **Do not edit by hand.** The `ninjaone-contract` CI job re-fetches the spec weekly and fails when this file would change — that failure is the signal to check whether a vendor change breaks an assumption in `model.rs`, `filter.rs` or `rows/`.

## Endpoints

### `/v2/activities`

- methods: GET
- parameters: after, before, class, df, endTime, lang, newerThan, olderThan, pageSize, seriesUid, sourceConfigUid, startTime, status, type, tz, user

### `/v2/automation/scripts`

- methods: GET
- parameters: lang

### `/v2/device/{id}/patch/os/apply`

- methods: POST
- parameters: id

### `/v2/device/{id}/patch/os/scan`

- methods: POST
- parameters: id

### `/v2/device/{id}/patch/software/apply`

- methods: POST
- parameters: id

### `/v2/device/{id}/patch/software/scan`

- methods: POST
- parameters: id

### `/v2/device/{id}/reboot/{mode}`

- methods: POST
- parameters: id, mode

### `/v2/device/{id}/script/run`

- methods: POST
- parameters: id

### `/v2/device/{id}/scripting/options`

- methods: GET
- parameters: id, lang

### `/v2/devices-detailed`

- methods: GET
- parameters: after, df, pageSize

### `/v2/locations`

- methods: GET
- parameters: after, pageSize

### `/v2/organizations`

- methods: GET, POST
- parameters: after, of, pageSize, templateOrganizationId

### `/v2/queries/os-patch-installs`

- methods: GET
- parameters: cursor, df, installedAfter, installedBefore, pageSize, status

### `/v2/queries/os-patches`

- methods: GET
- parameters: cursor, df, pageSize, severity, status, ts, type

### `/v2/queries/software-patch-installs`

- methods: GET
- parameters: cursor, df, impact, installedAfter, installedBefore, pageSize, productIdentifier, status, type

### `/v2/queries/software-patches`

- methods: GET
- parameters: cursor, df, impact, pageSize, productIdentifier, status, ts, type

### `/v2/roles`

- methods: GET

## Schemas this app deserializes

For each field the toolkit branches on: the vocabulary the spec declares, or **free-form** when it declares none. A free-form field is the vendor promising nothing — the parser must stay total and must not treat its own list as exhaustive.

### `Activity`

- `severity` — `NONE`, `MINOR`, `MODERATE`, `MAJOR`, `CRITICAL`
- `statusCode` — 675 values (count only: NinjaOne's whole activity vocabulary, which grows every release; `actions::is_action_activity` matches the handful the native action endpoints emit)
- `status` — **free-form string**, no enum declared
- `activityResult` — `SUCCESS`, `FAILURE`, `UNSUPPORTED`, `UNCOMPLETED`
- `type` — **free-form string**, no enum declared

### `DeviceOSPatch`

- `severity` — **free-form string**, no enum declared
- `status` — **free-form string**, no enum declared
- `type` — **free-form string**, no enum declared

### `DeviceSoftwarePatch`

- `status` — **free-form string**, no enum declared
- `type` — **free-form string**, no enum declared

### `Location`


### `NodeRole`


### `OSPatchReport`


### `Organization`


### `RunScriptRequest`

- `type` — `ACTION`, `SCRIPT`

### `Script`

- `type` — `ACTION`, `SCRIPT`

### `ScriptVariable`

- `type` — `CHECKBOX`, `DATE`, `DATETIME`, `DECIMAL`, `INTEGER`, `IPADDRESS`, `DROPDOWN`, `TEXT`, `OTHER`

### `SoftwarePatchReport`

