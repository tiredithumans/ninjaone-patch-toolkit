# Rust build artifacts
/target
/src-tauri/target
/web-rs/target

# Trunk / WASM frontend build output
/web-rs/dist

# Tauri generated sources
/src-tauri/gen/schemas

# Local secrets / env
.env
.env.*
!.env.example

# Screenshot tooling (Node/Playwright dev tool — see scripts/)
/scripts/node_modules

# Claude Code: per-session worktrees and personal (uncommitted) settings
/.claude/worktrees/
/.claude/settings.local.json

# Python bytecode (scripts/ninjaone-spec-digest.py)
__pycache__/

# OS / editor cruft
.DS_Store
*.swp
