# Claude Code Instructions — NinjaOne Patch Toolkit

All agent instructions live in [AGENTS.md](./AGENTS.md) — the single source of truth for every AI
assistant that contributes to this repo (Claude Code, Codex, Cursor, Aider, …). The `@` line below is
Claude Code's import syntax, so the full contents auto-load on session start; don't duplicate content
here.

@AGENTS.md
